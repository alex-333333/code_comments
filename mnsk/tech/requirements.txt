# Основные зависимости. Тяжёлых научных пакетов (numpy/scipy/pandas) намеренно нет:
# вся статистика (Манна-Уитни, хи-квадрат, Фишер, бутстреп) написана на чистом Python
# в stats.py и проверена тестами - установка быстрая и не ломается на новых версиях Python.
tree-sitter==0.26.0
# Версия закреплена: грамматики кэшируются по версии пакета, новая версия качает их заново.
tree-sitter-language-pack==1.16.2
requests>=2.31
# nltk нужен только ради SnowballStemmer: он не требует скачивания данных.
nltk>=3.9
# vaderSentiment (а не nltk.sentiment.vader): лексикон лежит внутри пакета,
# поэтому не нужен отдельный nltk.download().
vaderSentiment>=3.3.2
pytest>=8
